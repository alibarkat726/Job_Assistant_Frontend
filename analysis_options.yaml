# Analysis Options & Lint Rules for Job Assistant AI Platform
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  errors:
    invalid_annotation_target: ignore
    inference_failure_on_function_invocation: ignore
    inference_failure_on_instance_creation: ignore

# Code Review & Theme Architecture Rules:
# - NO literal Color(0x...) or Colors.xxx instantiations outside lib/core/theme/
# - NO direct GoogleFonts.xxx() calls outside lib/core/theme/app_typography.dart
# - ALL icons must use TablerIcons (flutter_tabler_icons)
# - ALL feature modules must consume AppSemanticColors via Theme.of(context).extension<AppSemanticColors>()!

linter:
  rules:
    public_member_api_docs: false
    lines_longer_than_80_chars: false
    always_use_package_imports: false
    sort_constructors_first: false
    always_put_required_named_parameters_first: false
    sort_pub_dependencies: false
    avoid_catches_without_on_clauses: false
    avoid_dynamic_calls: false
    prefer_initializing_formals: false
    avoid_redundant_argument_values: false
    unnecessary_lambdas: false
    unnecessary_underscores: false
    unnecessary_await_in_return: false
    avoid_classes_with_only_static_members: false
